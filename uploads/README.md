# Uploads directory
This folder structure is reserved for future server-side PHP file uploads (agreements, photos, documents).
In current LocalStorage mode, uploaded files are processed as Base64 Data URLs.
